export type TypeBaseProviderOptions = {
    name: string
    authorize_url: string
    access_url: string
    profile_url: string
    scopes: string[]
    cliend_id: string
    client_secret: string
}